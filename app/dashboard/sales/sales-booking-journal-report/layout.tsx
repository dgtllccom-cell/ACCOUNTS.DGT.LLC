import type { Metadata } from "next";

export const metadata: Metadata = { title: "Sales Booking Journal Report" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
