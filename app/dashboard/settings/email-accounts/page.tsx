import { EmailAccountsManagement } from "@/features/settings/components/email-accounts-management";

export const metadata = { title: "Settings — Email Accounts" };


export default function EmailAccountsPage() {
  return <EmailAccountsManagement />;
}
