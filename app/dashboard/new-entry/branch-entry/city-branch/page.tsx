import { CityBranchSetup } from "@/features/branches/components/city-branch-setup";

export const metadata = { title: "New Entry — Branch Entry — City Branch" };


export default function CityBranchPage() {
  return <CityBranchSetup />;
}

