// Promoted to the shared Person Master picker — see components/erp/person-picker.tsx.
// Re-exported from here so existing imports (employee-form.tsx, etc.) keep working unchanged.
export { PersonPicker, personFullName } from "@/components/erp/person-picker";
