# ============================================================================
# PRODUCTION environment  →  PROD Supabase project (inmayhrxucimxqhgseqi)
#
# HOW TO USE:
#   1. Fill the two <PLACEHOLDER> values below (see notes).
#   2. Copy the contents into the VPS file:  /var/www/dgt-nextjs/.env.local
#      (the deploy script preserves .env.local, so it survives future deploys)
#   3. Restart:  pm2 restart dgt-nextjs
#
# This file is a TEMPLATE — do not put the real secrets in the git repo.
# ============================================================================

APP_ENV=production
NODE_ENV=production

# --- Supabase (PROD project) ---
NEXT_PUBLIC_SUPABASE_URL=https://inmayhrxucimxqhgseqi.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_96Wge0CK7yyU3_NaeQ-qhw_1GxU3Dxw

# service_role SECRET — get it from:
#   Supabase Dashboard → ACCOUNTS.DGT.LLC - Production → Project Settings → API
#   → "service_role" (secret). Long "eyJ..." JWT or "sb_secret_..." value.
SUPABASE_SERVICE_ROLE_KEY=<PROD_SERVICE_ROLE_SECRET>

# --- Database (PROD — Supabase transaction pooler) ---
# Set/reset the DB password at:
#   Supabase Dashboard → ACCOUNTS.DGT.LLC - Production → Project Settings
#   → Database → "Reset database password", then URL-encode special chars.
DATABASE_URL=postgresql://postgres.inmayhrxucimxqhgseqi:<PROD_DB_PASSWORD>@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres
