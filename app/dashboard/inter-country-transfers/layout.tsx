import type { Metadata } from "next";

export const metadata: Metadata = { title: "Inter-Country Transfers" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
