import type { Metadata } from "next";

export const metadata: Metadata = { title: "General Office — Employees" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
