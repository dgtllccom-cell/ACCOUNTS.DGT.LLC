import { ShippingLineMasterSetup } from "@/features/shipping/components/shipping-line-master-setup";

export const metadata = { title: "New Entry — Shipping Line" };

export default function ShippingLineEntryPage() {
  return <ShippingLineMasterSetup />;
}
