import type { Metadata } from "next";

export const metadata: Metadata = { title: "Settings — Translations" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
