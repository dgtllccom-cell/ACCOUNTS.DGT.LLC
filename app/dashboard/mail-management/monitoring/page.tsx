"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import {
  AlertCircle,
  AlertTriangle,
  ArrowLeft,
  CheckCircle2,
  Cpu,
  Database,
  Globe,
  HardDrive,
  KeyRound,
  RefreshCw,
  Server,
  ShieldAlert,
  ShieldCheck,
  Terminal,
  Zap,
} from "lucide-react";
import { DashboardFrame } from "@/components/layout/dashboard-frame";

export default function DgtMailMonitoringPage() {
  const [loading, setLoading] = useState(false);
  const [serverInfo, setServerInfo] = useState<any>({
    online: true,
    version: "Stalwart v0.8.0 (x86_64-unknown-linux-gnu)",
    hostname: "mail.dgt.llc",
    ip: "72.60.209.121",
    storagePath: "/var/lib/stalwart-mail",
    activeConnections: 1,
    inboundQueue: 0,
    outboundQueue: 0,
  });

  const dnsChecklist = [
    {
      name: "A Record (mail.dgt.llc)",
      expected: "72.60.209.121",
      status: "Ready",
      detail: "Points mail server hostname to Hostinger VPS",
      icon: CheckCircle2,
      color: "text-emerald-500",
    },
    {
      name: "Reverse DNS / PTR (Hostinger VPS)",
      expected: "mail.dgt.llc",
      status: "Configuration Required",
      detail: "Set in Hostinger VPS Dashboard -> IP Management -> Set PTR to mail.dgt.llc",
      icon: AlertTriangle,
      color: "text-amber-500",
    },
    {
      name: "SPF Record (TXT @)",
      expected: "v=spf1 ip4:72.60.209.121 include:spf.titan.email ~all",
      status: "Ready for Publishing",
      detail: "Authorizes both VPS 72.60.209.121 and Titan Email to send @dgt.llc emails",
      icon: CheckCircle2,
      color: "text-emerald-500",
    },
    {
      name: "DKIM Key (TXT default._domainkey.dgt.llc)",
      expected: "v=DKIM1; k=rsa; p=MIIBIjANBgkqhkiG9w0BAQEFA...",
      status: "Generated by Stalwart",
      detail: "Cryptographically signs all outbound emails so Gmail/Yahoo verify legitimacy",
      icon: CheckCircle2,
      color: "text-emerald-500",
    },
    {
      name: "DMARC Policy (TXT _dmarc.dgt.llc)",
      expected: "v=DMARC1; p=quarantine; rua=mailto:postmaster@dgt.llc",
      status: "Ready for Publishing",
      detail: "Protects domain reputation and catches spoofed messages",
      icon: CheckCircle2,
      color: "text-emerald-500",
    },
    {
      name: "MX Record (Root / Subdomain)",
      expected: "10 mail.dgt.llc",
      status: "Pending Final Cutover",
      detail: "Routes all incoming external emails (including social OTPs) to VPS Stalwart daemon",
      icon: AlertTriangle,
      color: "text-amber-500",
    },
  ];

  const ports = [
    { port: 25, protocol: "SMTP", direction: "Inbound", status: "Listening", desc: "Incoming external mail from Gmail/Outlook/Yahoo" },
    { port: 587, protocol: "SMTP Submission", direction: "Outbound", status: "Listening (STARTTLS)", desc: "Mail client secure message dispatch" },
    { port: 465, protocol: "SMTPS", direction: "Outbound", status: "Listening (TLS/SSL)", desc: "Implicit TLS submission" },
    { port: 993, protocol: "IMAPS", direction: "Inbound/Client", status: "Listening (TLS/SSL)", desc: "Secure mailbox synchronization" },
  ];

  return (
    <DashboardFrame>
      <div className="space-y-6">
        {/* Header */}
        <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 pb-4 border-b border-slate-200 dark:border-slate-800">
          <div className="flex items-center gap-3">
            <Link
              href="/dashboard/mail-management"
              className="p-2 rounded-xl border border-slate-200 dark:border-slate-800 hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-600 transition-colors"
            >
              <ArrowLeft className="h-4 w-4" />
            </Link>
            <div>
              <h1 className="text-xl font-bold tracking-tight text-slate-900 dark:text-white">
                Mail Server & Deliverability Health
              </h1>
              <p className="text-xs text-slate-500 mt-0.5">
                Stalwart Mail Server &bull; Port Diagnostics &bull; DNS Authentication (SPF, DKIM, DMARC, rDNS)
              </p>
            </div>
          </div>
        </div>

        {/* Server Daemon Card */}
        <div className="rounded-2xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 p-6 shadow-sm">
          <div className="flex items-center justify-between pb-4 border-b border-slate-100 dark:border-slate-800">
            <div className="flex items-center gap-3">
              <div className="h-10 w-10 rounded-xl bg-emerald-50 dark:bg-emerald-950/50 text-emerald-600 flex items-center justify-center">
                <Server className="h-5 w-5" />
              </div>
              <div>
                <h3 className="text-base font-bold text-slate-900 dark:text-white flex items-center gap-2">
                  <span>Stalwart Mail Server Engine</span>
                  <span className="px-2 py-0.5 rounded-full text-[10px] font-bold bg-emerald-100 text-emerald-700">
                    Active
                  </span>
                </h3>
                <p className="text-xs text-slate-400 font-mono">
                  Host: {serverInfo.hostname} ({serverInfo.ip}) &bull; Linux Ubuntu 24.04 LTS
                </p>
              </div>
            </div>

            <div className="text-right">
              <span className="text-xs font-semibold text-slate-700 dark:text-slate-300 block">
                {serverInfo.version}
              </span>
              <span className="text-[11px] text-slate-400 font-mono">
                Storage: {serverInfo.storagePath}
              </span>
            </div>
          </div>

          {/* Protocol Ports Grid */}
          <div className="mt-5 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3">
            {ports.map((p) => (
              <div key={p.port} className="p-3.5 rounded-xl bg-slate-50 dark:bg-slate-800/40 border border-slate-200 dark:border-slate-800">
                <div className="flex items-center justify-between">
                  <span className="font-mono text-sm font-bold text-slate-900 dark:text-white">Port {p.port}</span>
                  <span className="text-[10px] font-semibold text-emerald-600 dark:text-emerald-400">{p.status}</span>
                </div>
                <div className="text-xs font-medium text-slate-700 dark:text-slate-300 mt-1">{p.protocol}</div>
                <p className="text-[10px] text-slate-400 mt-1">{p.desc}</p>
              </div>
            ))}
          </div>
        </div>

        {/* DNS & Anti-Spam Deliverability Matrix */}
        <div className="rounded-2xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 p-6 shadow-sm space-y-4">
          <div className="flex items-center justify-between pb-3 border-b border-slate-100 dark:border-slate-800">
            <div>
              <h3 className="text-base font-bold text-slate-900 dark:text-white flex items-center gap-2">
                <Globe className="h-4 w-4 text-blue-600" />
                <span>DNS Reputation & Deliverability Matrix</span>
              </h3>
              <p className="text-xs text-slate-500 mt-0.5">
                Required records to guarantee delivery directly to recipient Inboxes on Gmail, Yahoo, Outlook, and prevent spoofing.
              </p>
            </div>
          </div>

          <div className="divide-y divide-slate-100 dark:divide-slate-800/70">
            {dnsChecklist.map((item, idx) => {
              const Icon = item.icon;
              return (
                <div key={idx} className="py-3.5 flex flex-col md:flex-row md:items-center justify-between gap-3">
                  <div className="space-y-1 max-w-xl">
                    <div className="flex items-center gap-2">
                      <Icon className={`h-4 w-4 shrink-0 ${item.color}`} />
                      <span className="text-xs font-bold text-slate-900 dark:text-white">{item.name}</span>
                      <span className="text-[10px] px-2 py-0.5 rounded bg-slate-100 dark:bg-slate-800 text-slate-600 font-medium">
                        {item.status}
                      </span>
                    </div>
                    <p className="text-xs text-slate-500 pl-6">{item.detail}</p>
                  </div>

                  <div className="pl-6 md:pl-0">
                    <code className="text-[11px] font-mono bg-slate-100 dark:bg-slate-800 px-3 py-1.5 rounded-lg text-slate-800 dark:text-slate-200 select-all block max-w-md truncate">
                      {item.expected}
                    </code>
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        {/* Storage Expansion Guide */}
        <div className="rounded-2xl border border-slate-200 dark:border-slate-800 bg-gradient-to-r from-blue-50 to-indigo-50 dark:from-blue-950/20 dark:to-indigo-950/20 p-6 shadow-sm">
          <div className="flex items-start gap-3">
            <div className="h-10 w-10 rounded-xl bg-blue-600 text-white flex items-center justify-center shrink-0 shadow-sm">
              <Database className="h-5 w-5" />
            </div>
            <div>
              <h3 className="text-sm font-bold text-slate-900 dark:text-white">
                Multi-Server Scaling & Storage Expansion
              </h3>
              <p className="text-xs text-slate-600 dark:text-slate-300 mt-1 leading-relaxed">
                As the number of public mail accounts grows, mail storage on Hostinger VPS can be expanded in two zero-downtime ways:
              </p>
              <ul className="mt-3 space-y-1.5 text-xs text-slate-600 dark:text-slate-300 list-disc list-inside">
                <li>
                  <strong>Attached NVMe Block Storage:</strong> Mount additional Hostinger block storage volumes at <code className="font-mono bg-white/60 dark:bg-slate-800/80 px-1.5 py-0.5 rounded">/var/lib/stalwart-mail/blobs</code>.
                </li>
                <li>
                  <strong>S3 / MinIO Object Storage:</strong> Configure Stalwart to offload large attachments and multimedia directly to remote cloud buckets while retaining lightning-fast local indexing.
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </DashboardFrame>
  );
}
